spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
    stream:
      binders:
        product_stock:
          group: order
          content-type: application/json
  redis:
    host: 192.168.1.113
    port: 6379
